# Local security notes for this portfolio (no secrets stored here).
#
# Contact info on the site is intentionally public (CV portfolio).
# Secrets for apps (Apple .p8, Firebase Admin JSON, .env) live in:
#   ~/.secrets/
# Never copy those into this repo or public/screenshots.
